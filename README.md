# How to

See the `docs` folder for instructions on specific L1 topics; the L1 directory
`.firefox` corresponds to files matching the pattern `docs/firefox-*`. Although
there are some exceptions where naming limits apply.

## Installing a configuration (`charon install`)

> Never spend 6 minutes doing something by hand when you can spend 6 hours
  failing to automate it

See https://github.com/skeletony007/charon for up-to-date information on using
charon to automatically apply/remove configurations.

